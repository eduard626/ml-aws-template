# Copy this to .env (which is gitignored) for local development',
MLFLOW_TRACKING_URI=http://127.0.0.1:5000
MLFLOW_EXPERIMENT_NAME=My-Default-Experiment
MLFLOW_MODEL_NAME=my-project-model
# AWS credentials are typically handled by the AWS CLI profile (e.g., ~/.aws/credentials)
DVC_S3_BUCKET_NAME=my-ml-s3-bucket-name